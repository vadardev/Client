export class UserCardModel {
    constructor(public userCardId: string, public word: string, public definition: string, public pictureUrl: string, public example: string, public otherExample: string) { };
}